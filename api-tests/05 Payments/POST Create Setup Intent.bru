meta {
  name: POST Create Setup Intent
  method: POST
  type: http
  url: {{base_url}}/api/payments/setup-intent
}

headers {
  Authorization: Bearer {{auth_token}}
  Content-Type: application/json
}

body:json {
  {
    "bookingId": "{{booking_id}}"
  }
}
