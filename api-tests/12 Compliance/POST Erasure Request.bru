meta {
  name: POST Erasure Request
  method: POST
  type: http
  url: {{base_url}}/api/compliance/erasure
}

headers {
  Authorization: Bearer {{auth_token}}
  Content-Type: application/json
}

body:json {
  {
    "email": "{{patient_email}}"
  }
}
