meta {
  name: POST Export Patient Data
  method: POST
  type: http
  url: {{base_url}}/api/compliance/export
}

headers {
  Authorization: Bearer {{auth_token}}
  Content-Type: application/json
}

body:json {
  {
    "email": "{{patient_email}}"
  }
}
