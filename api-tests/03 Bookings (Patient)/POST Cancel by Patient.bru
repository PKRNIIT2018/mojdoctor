meta {
  name: POST Cancel by Patient
  method: POST
  type: http
  url: {{base_url}}/api/bookings/{{booking_id}}/cancel-patient
}

headers {
  Authorization: Bearer {{auth_token}}
  Content-Type: application/json
}

body:json {
  {
    "patientEmail": "{{patient_email}}"
  }
}
