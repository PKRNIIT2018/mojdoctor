meta {
  name: POST Create Video Room
  method: POST
  type: http
  url: {{base_url}}/api/video/rooms
}

headers {
  Authorization: Bearer {{auth_token}}
  Content-Type: application/json
}

body:json {
  {
    "bookingId": "{{booking_id}}"
  }
}
