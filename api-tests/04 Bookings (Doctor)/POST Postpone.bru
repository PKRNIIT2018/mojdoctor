meta {
  name: POST Postpone
  method: POST
  type: http
  url: {{base_url}}/api/bookings/postpone
}

headers {
  Authorization: Bearer {{auth_token}}
  Content-Type: application/json
}

body:json {
  {
    "bookingId": "{{booking_id}}",
    "alternativeSlotIds": ["{{slot_id}}"]
  }
}
