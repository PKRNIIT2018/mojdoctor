meta {
  name: POST Reject Booking
  method: POST
  type: http
  url: {{base_url}}/api/bookings/reject
}

headers {
  Authorization: Bearer {{auth_token}}
  Content-Type: application/json
}

body:json {
  {
    "bookingId": "{{booking_id}}",
    "reason": "Schedule conflict"
  }
}
